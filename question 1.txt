1. How we can use ssh in github and also write command to generate ssh keys in your respestive operating system.

Ans >
SSH (source shell protocol ) is used to commit changes in the git hub respo by using ssh key of your local pc. 

we can use ssh in  github by:-
1)Copy the SSH public key from local pc.
2)Then Open github account and open setting init.
3)In setting open SSH and GPG keys.
4)There add new ssh key and paste the copied ssh key .

Command to generate ssh keys is:-
1)ssh-keygen in cmd 



 
